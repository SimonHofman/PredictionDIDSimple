{
  "name": "prediction-did-contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "seed:local": "hardhat run scripts/seed-markets.js --network localhost",
    "resolve:local": "hardhat run scripts/resolve.js --network localhost",
    "coverage": "hardhat coverage",
    "export-abi": "node scripts/export-abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.17",
    "solidity-coverage": "^0.8.14"
  }
}
